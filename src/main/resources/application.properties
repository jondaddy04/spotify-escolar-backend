# ============================================================
# SPOTIFY ESCOLAR - Configuración Principal
# ============================================================

# Servidor
server.port=8080

# Base de datos (LOCAL - cambia en Render con variables de entorno)
spring.datasource.url=jdbc:postgresql://localhost:5432/spotify_escolar
spring.datasource.username=postgres
spring.datasource.password=admin123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Inicializar base de datos con el script SQL
spring.sql.init.mode=never
spring.sql.init.platform=postgresql

# Archivos estáticos (audio e imágenes)
spring.web.resources.static-locations=classpath:/static/
spring.mvc.static-path-pattern=/**
# JWT
app.jwt.secret=SpotifyEscolarSecretKeyMuyLargaParaQueSeaSegura2024XYZ
app.jwt.expiration-ms=86400000

# Correo (configurar con tus datos reales)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=joncruk2004@gmail.com
spring.mail.password=ppwqazvjrgmiclrm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Cloudinary
cloudinary.cloud-name=dksuvhkd4
cloudinary.api-key=782525376115556
cloudinary.api-secret=y_8dUM-STfM2-bvs9GN2O7Cpq3I

# URL base del backend (para construir URLs de audio/imágenes)
app.base-url=http://localhost:8080

# CORS - orígenes permitidos
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://localhost:19006,exp://localhost:19000

# Logging
logging.level.com.spotify.escolar=INFO
logging.level.org.springframework.security=WARN
